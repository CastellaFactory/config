export EDITOR="vim"
export VISUAL="vim"
export ALTERNATE_EDITOR=""

if [ -e /usr/local/etc/autojump.zsh ]; then
	source /usr/local/etc/autojump.zsh
fi

source $ZDOTDIR/.zshenv

export AUTOJUMP_IGNORE_CASE=1

# vim: ft=zsh
